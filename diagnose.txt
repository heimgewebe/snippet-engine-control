Befund:
Phase 0 bis 5, 7 und 8 sind im Repo vollständig umgesetzt (Workspace-Model existiert, E2E-Tests durchlaufen, UI ist angeschlossen, Apply-Rollback existiert, Undo/Redo existiert).

Phase 6 ("Preview ausbauen") ist nur teilweise umgesetzt.
- [x] 1. static (in `simulateExpansion`)
- [x] 2. template-aware (in `simulateExpansion` regexp check)
- [ ] 3. engine-aware (Espanso port fehlt)
- [ ] 4. expansion trace (Trace-Logik fehlt)

Begründung der Roadmap-Einschätzung & Optimierung:
Die geplante Roadmap für Phase 6 ist korrekt und passend. Da das Grundgerüst (Ports in `packages/app/src/ports/engine.ts` existieren) steht, ist der beste nächste architektonische Hebel, den `EnginePreviewPort` im `adapter-espanso` (als MVP) zu implementieren und im CLI-Daemon bereitzustellen, damit die UI davon profitiert. Es bedarf keiner großen Neuarchitektur, sondern nur der gezielten Umsetzung des Ports.
